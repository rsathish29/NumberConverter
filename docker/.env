# ./docker/.env
# This is the env file that is used by the docker compose file by creating various services(Containers)
COMPOSE_PROJECT_NAME=spring_boot_elk_demo
ELK_VERSION=7.6.1

# Note that there is currently no known way to change the default Elasticsearch username!
ELASTIC_USER=elastic
ELASTIC_PASSWORD=changeme

ROMANCONVERSION_VERSION=0.0.1-SNAPSHOT
ROMANCONVERSION_JAR_NAME=roman-conversion-0.0.1-SNAPSHOT.jar
